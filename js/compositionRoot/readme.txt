CompositionRoot, which is the app's entry point, is responsible for combining Model, View and Controller.
FMCompositionRoot is responsible for combining all models.
FVCompositionRoot is responsible for combining all views.